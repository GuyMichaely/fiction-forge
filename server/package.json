{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "proxy": "http://localhost:5000",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --unhandled-rejections=strict server.js",
    "dev": "nodemon --inspect=0.0.0.0:9229 --trace-warnings --unhandled-rejections=strict server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.8.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
